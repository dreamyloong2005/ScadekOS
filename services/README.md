# ScadekOS Services

This directory is for OS-level service composition and policy.

Kernel-resident SCDK services still live in the SCDK submodule. ScadekOS-owned
service launch policy should use SCDK-native endpoints, messages, capabilities,
rings, and grants.


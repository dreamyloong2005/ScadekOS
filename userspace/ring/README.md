# ScadekOS Ring Demo

`ring.S` creates a user grant, creates and binds a ring to the console endpoint,
submits 16 console-write descriptors, and polls 16 completions.

This is the current ScadekOS data-plane demo for SCDK M24.

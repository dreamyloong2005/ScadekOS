# ScadekOS Ring Demo

`ring.S` uses `libscadek` to create a user grant, create and bind a ring to the
console endpoint, submit 16 console-write descriptors, and poll 16 completions.

This is the current ScadekOS data-plane demo for SCDK M30.
